/**
 * Created by Ryan Owens on 6/14/16.
 */
/*function Blanket(startingX, startingY) {
 Mark.call(this, 'blanket', x, y);

 this.draw = function(context) {
 context.globalAlpha = this.alpha;
 context.strokeStyle = "#5555AA";
 context.fillStyle = "#7777CC";
 context.lineWidth = 3;
 context.beginPath();
 if (this.points.length < 0)
 return;
 context.moveTo(this.points[0].x * scale, this.points[0].y * scale);
 for (i = 0; i < this.points.length; i++) {
 context.lineTo(this.points[i].x * scale, this.points[i].y * scale);
 }
 if (this == selectedMark) {
 context.lineTo(mousePosition.x * scale, mousePosition.y * scale);
 }
 context.lineTo(this.points[0].x * scale, this.points[0].y * scale);
 context.stroke();
 context.globalAlpha *= .3;
 context.fill();
 };
 }*/