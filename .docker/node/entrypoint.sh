#!/bin/sh

# If you add more to this script, make sure the following line is either last or add an & after it to make it parallel
sass --watch /app/csb/csb-themes/default/sass/style.scss:/app/csb/csb-themes/default/style.css